{
  "content": "import React from 'react';\\nimport { useRouter } from 'next/router';\\nimport { useForm } from 'react-hook-form';\\n\\nexport default function Login() {\\n  const { register, handleSubmit } = useForm();\\n  const router = useRouter();\\n\\n  const onSubmit = data => {\\n    console.log(data);\\n    // Perform login\\n    // On success:\\n    router.push('/details');\\n  };\\n\\n  return (\\n    <form onSubmit={handleSubmit(onSubmit)} className='flex flex-col gap-4'>\\n      <input {...register('username')} placeholder='Username' className='border p-2' />\\n      <input {...register('password')} type='password' placeholder='Password' className='border p-2' />\\n      <button type='submit' className='border p-2 bg-blue-500 text-white'>Login</button>\\n    </form>\\n  );\\n}"
}